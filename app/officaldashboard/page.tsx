"use client";

import OfficialDashboard from "@/components/officaldashboard";

export default function OfficalDashboardPage() {
  // You can change the level prop as needed
  return <OfficialDashboard level={1} />;
} 